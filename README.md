## Создание проекта

## Узнайте, как создать новый проект на Expo.

Expo — это фреймворк для React Native, упрощающий разработку приложений для Android и iOS. Наш фреймворк предоставляет маршрутизацию на основе файлов, стандартную библиотеку нативных модулей и многое другое. Expo — это проект с открытым исходным кодом и активным сообществом на GitHub и Discord.

Мы также разрабатываем Expo Application Services (EAS) — набор сервисов, дополняющих фреймворк Expo на каждом этапе процесса разработки.

    Только начинаете программировать? Вы можете создать свое первое приложение на Expo, используя AI-агента для написания кода, вместо того чтобы писать его вручную. Ознакомьтесь с руководством «Создание с помощью ИИ» (Build with AI). В нем описан процесс настройки с нуля.

Системные требования

    Node.js (LTS).
    Поддерживаются macOS, Windows (PowerShell и WSL 2) и Linux.

## Начало работы со стандартным проектом

Мы рекомендуем начать со стандартного проекта, создаваемого с помощью `create-expo-app`. Этот проект содержит примеры кода, которые помогут вам быстрее освоиться.

Чтобы создать новый проект, выполните следующую команду:
Терминал
npm
yarn
pnpm
bun

npx create-expo-app@latest

    Вы можете выбрать другой шаблон, добавив опцию `--template`.

## Начало работы с примером

Вместо стандартного проекта можно начать с одного из примеров Expo. Это небольшие приложения, каждое из которых демонстрирует определенную функцию или интеграцию, например Expo Router, Expo Widgets или экран камеры.

Чтобы просмотреть полный список и выбрать пример в интерактивном режиме, запустите `create-expo-app` с опцией `--example` без указания имени:
Терминал
npm
yarn
pnpm
bun

npx create-expo-app@latest --example

Чтобы создать конкретный пример, укажите его имя:
Терминал
npm
yarn
pnpm
bun

npx create-expo-app@latest --example with-widgets

    Остальные руководства в этом разделе («Начало работы») ориентированы на стандартный проект. Структура приложения-примера может отличаться, но используемые концепции остаются прежними.

## Настройка AI-агента

Новый проект включает файлы контекста, которые считывают AI-агенты: `AGENTS.md`, `CLAUDE.md` и `.claude/settings.json`. Для Claude Code и Codex также существуют официальные плагины Expo. С помощью одной команды можно установить Expo Skills и зарегистрировать сервер Expo Model Context Protocol (MCP):

Claude Code

Codex
Терминал

claude plugin install expo@claude-plugins-official

Затем выполните команду `/mcp` в сеансе Claude Code, чтобы войти в свою учетную запись Expo.

Плагин также устанавливает навык, который помогает агенту ориентироваться в структуре файлов нового проекта Expo:
expo-project-structure

## Структура папок нового приложения Expo.

Для Cursor и других агентов устанавливайте Expo Skills и сервер Expo MCP по отдельности. В разделе «AI-агенты и обзор Expo» описана настройка для каждого агента.
Следующий шаг

У вас есть проект. Теперь пришло время настроить среду разработки, чтобы можно было приступить к созданию приложения.

## Настройка окружения

Узнайте, как настроить среду разработки, чтобы начать создавать приложения с помощью Expo.

Давайте настроим локальную среду разработки для запуска вашего проекта на Android и iOS.
На каком устройстве вы хотите вести разработку?

Мы рекомендуем использовать реальное устройство, так как вы сможете увидеть именно то, что увидят ваши пользователи.
Устройство Android

Устройство Android
Устройство iOS

Устройство iOS
Эмулятор Android

Эмулятор Android
Симулятор iOS

Симулятор iOS
Какой способ разработки вы предпочитаете?

Expo Go — это площадка, где студенты и начинающие разработчики могут быстро опробовать возможности Expo. Development build (сборка для разработки) — это версия вашего собственного приложения, включающая инструменты разработки Expo.

## Начните разработку

Внесите первое изменение в проект Expo и сразу увидите результат на своем устройстве.

Запустите сервер разработки

1
Чтобы запустить сервер разработки, выполните следующую команду:
Терминал
npm
yarn
pnpm
bun

npx expo start

2
Откройте приложение на устройстве

После выполнения команды выше в терминале появится QR-код. Отсканируйте его, чтобы открыть приложение на своем устройстве.

Если вы используете эмулятор Android или симулятор iOS, можно нажать клавишу A или I соответственно, чтобы открыть приложение.
Возникли проблемы?

Убедитесь, что компьютер и устройство подключены к одной сети Wi-Fi.

Если это не помогло, проблема может быть в настройках маршрутизатора (часто встречается в публичных сетях). Решить проблему можно, выбрав тип подключения «Tunnel» при запуске сервера разработки, а затем снова отсканировав QR-код.
Терминал
npm
yarn
pnpm
bun

npx expo start --tunnel

    При использовании типа подключения «Tunnel» приложение будет перезагружаться значительно медленнее, чем в режимах LAN или Local, поэтому по возможности этого режима лучше избегать. Если же использование «Tunnel» необходимо для доступа к вашему компьютеру с другого устройства в сети, для ускорения разработки можно воспользоваться эмулятором или симулятором.

3
Внесите первое изменение

Откройте файл `src/app/index.tsx` в редакторе кода и внесите изменения.

Дальнейшие шаги

Разработка, проверка и публикация проекта.

Вот следующие шаги по созданию вашего приложения:
Сброс проекта

Вы можете удалить шаблонный код и начать работу над проектом с чистого листа. Выполните следующую команду для сброса проекта:
Терминал
npm
yarn
pnpm
bun

npm run reset-project

Эта команда переместит существующие файлы из папки `app` в `app-example`, а затем создаст новую директорию `app` с новым файлом `index.tsx`.
Разработка, проверка и развертывание

Изучите процесс разработки, ознакомившись с документацией в разделе «Разработка» (Develop). Вы узнаете, как создавать элементы пользовательского интерфейса, добавлять модульные тесты, подключать нативные модули и многое другое.

После завершения разработки вы сможете показать приложение коллегам для проверки.

Наконец, вы сможете собрать проект и отправить его в магазины приложений.
Пошаговое руководство

Чтобы пройти весь путь создания приложения на Expo — от начала до конца — с подробными инструкциями, ознакомьтесь с обучающим руководством.

## Инструменты для разработки

Обзор инструментов и веб-ресурсов Expo, которые помогут вам на разных этапах создания проекта.

При создании нового проекта на Expo полезно ознакомиться с основными инструментами и ресурсами, которые упростят процесс разработки приложения. На этой странице представлен список рекомендуемых инструментов.
Expo CLI

Expo CLI — это инструмент разработки, который устанавливается автоматически вместе с пакетом `expo` при создании нового проекта. Для его запуска можно использовать `npx` (инструмент для выполнения пакетов Node.js).

Он призван ускорить процесс разработки приложения. Например, первое взаимодействие с Expo CLI обычно происходит при запуске сервера разработки командой `npx expo start`.

Ниже приведен список часто используемых команд Expo CLI:
Команда	Описание
npx expo start	Запускает сервер разработки (независимо от того, используете ли вы специальную сборку для разработки или Expo Go).
npx expo prebuild	Генерирует нативные директории Android и iOS с помощью функции Prebuild.
npx expo run:android	Компилирует нативное приложение Android локально.
npx expo run:ios	Компилирует нативное приложение iOS локально.
npx expo install package-name	Используется для установки новой библиотеки или для проверки и обновления конкретных библиотек в проекте (при добавлении опции `--fix`).
npx expo lint	Настраивает ESLint. Если ESLint уже настроен, эта команда выполняет проверку (linting) файлов проекта.

Вкратце, Expo CLI позволяет разрабатывать, компилировать и запускать приложение, а также выполнять другие задачи. Подробную информацию о доступных опциях и действиях можно найти в справочном руководстве по Expo CLI.
EAS CLI

EAS CLI используется для входа в учетную запись Expo и компиляции приложения с помощью различных сервисов EAS, таких как Build, Update или Submit. Этот инструмент также позволяет:

    Публиковать приложение в магазинах приложений
    Создавать сборки приложения для разработки, предварительного просмотра или продакшена
    Создавать обновления «по воздуху» (OTA)
    Управлять учетными данными приложения
    Создавать профиль подготовки (provisioning profile) типа Ad Hoc для устройств iOS

Чтобы использовать EAS CLI, необходимо установить его глобально на локальном компьютере, выполнив следующую команду:
Терминал
npm
yarn
pnpm
bun

npm install --global eas-cli

Вы можете использовать команду `eas --help` в окне терминала, чтобы узнать больше о доступных командах. Полное справочное руководство доступно на странице eas-cli в npm.

Expo Doctor

Expo Doctor — это инструмент командной строки для диагностики проблем в проекте Expo. Чтобы воспользоваться им, выполните следующую команду в корневом каталоге проекта:
Терминал
npm
yarn
pnpm
bun

npx expo-doctor

Эта команда выполняет проверку и анализ кодовой базы проекта на предмет распространенных проблем: ошибок в конфигурации приложения и файле `package.json`, несовместимости зависимостей, некорректных настроек в конфигурационных файлах, а также общего состояния проекта. По завершении проверки Expo Doctor выводит результаты.

Если Expo Doctor обнаружит проблему, он предоставит ее описание, а также рекомендации по устранению или информацию о том, где можно найти помощь.

По умолчанию Expo Doctor сверяет пакеты вашего проекта с данными из каталога React Native и проверяет синхронизацию свойств конфигурации приложения при наличии нативных директорий. Эти проверки можно настроить в файле `package.json` вашего проекта. Подробности см. в описании параметров `reactNativeDirectoryCheck` и `appConfigFieldsNotSyncedCheck`.

Также можно использовать команду `npx expo-doctor --help` для просмотра справочной информации.
Orbit

Orbit — это приложение для macOS, Windows и Linux, которое позволяет:

    Устанавливать и запускать сборки из EAS на физических устройствах и эмуляторах.
    Устанавливать и запускать обновления из EAS на эмуляторах Android или симуляторах iOS.
    Запускать проекты Snack на эмуляторах Android или симуляторах iOS.
    Использовать локальные файлы для установки и запуска приложений. Orbit поддерживает любые файлы `.apk` (Android), приложения `.app`, совместимые с симулятором iOS, а также приложения, подписанные для Ad Hoc распространения.
    Просматривать список закрепленных проектов из панели управления EAS.
    Установка

macOS

Windows

Linux

Orbit можно установить через Homebrew (для macOS) или загрузить напрямую из раздела релизов на GitHub.
Терминал

brew install expo-orbit

Чтобы Orbit запускался автоматически при входе в систему, нажмите на значок Orbit в строке меню, перейдите в раздел Settings (Настройки) и выберите опцию Launch on Login (Запускать при входе).

    Для работы Orbit на macOS, Windows и Linux требуется Android SDK, а на macOS для управления устройствами также необходим xcrun; это подразумевает установку как Android Studio, так и Xcode.

## Expo Tools для VS Code

Expo Tools — это расширение для VS Code, упрощающее процесс разработки при работе с файлами конфигурации приложений. Оно предоставляет такие функции, как автодополнение и IntelliSense, для файлов конфигурации приложения (app config), EAS, магазина приложений (store config) и модулей Expo.
Установка расширения Expo Tools для VS Code

Воспользуйтесь этой ссылкой для установки расширения или найдите Expo Tools непосредственно в редакторе VS Code.

Расширение также позволяет отлаживать приложение с помощью встроенного отладчика VS Code: устанавливать точки останова, просматривать значения переменных, выполнять код в консоли отладки и многое другое. Подробнее об использовании расширения для отладки читайте в разделе «Отладка в VS Code».
Тестирование прототипов с помощью Snack и Expo Go
Snack

Snack — это среда разработки, работающая прямо в браузере и функционирующая аналогично Expo Go. Это отличный способ делиться фрагментами кода и экспериментировать с React Native без установки каких-либо инструментов на компьютер.

Чтобы воспользоваться инструментом, перейдите на сайт snack.expo.dev, измените компонент `<Text>` в файле `App.js`, выберите платформу (Android, iOS или веб) на правой панели и наблюдайте за изменениями в реальном времени.

## Expo Go

Expo Go — это бесплатная среда с открытым исходным кодом, позволяющая студентам и тем, кто учится программировать, опробовать React Native в деле. Она работает на Android и iOS.

Чтобы узнать больше о том, как пользоваться этим инструментом:

    Перейдите по ссылке к руководству по настройке окружения (Set up your environment).
    Выберите платформу в разделе «Where would you like to develop?» (Где вы хотите вести разработку?).
    Выберите Expo Go в разделе «How would you like to develop?» (Как вы хотите вести разработку?).
    Следуйте инструкциям, приведенным в этом руководстве.

    Примечание: возможности Expo Go ограничены, и этот инструмент не подходит для создания проектов промышленного уровня (production-grade). Вместо него используйте сборки для разработки (development builds).

expo-go CLI

expo-go CLI — это автономный инструмент, который загружает бинарный файл Expo Go для выбранной платформы и конкретной версии SDK либо выводит URL для его скачивания. Укажите версию SDK, чтобы зафиксировать конкретный релиз (или пропустите этот параметр для загрузки последней версии), а также укажите платформу, чтобы скачать нужный бинарный файл.
Терминал
npm
yarn
pnpm
bun
npx expo-go download android latest
npx expo-go url ios latest

Эта команда загружает приложение Expo Go в текущую директорию и кэширует его в папке `~/.expo`.

    Инструмент expo-go CLI работает с устройствами Android, эмулятором Android и симулятором iOS. Из-за политики Apple установка (sideloading) старых версий приложений на реальные iPhone, как правило, не поддерживается, и expo-go CLI также не обеспечивает такую ​​возможность.

Что делать, если я открываю проект с неподдерживаемой версией SDK?

При запуске в Expo Go проекта, созданного для неподдерживаемой версии SDK, вы увидите следующую ошибку:

«Project is incompatible with this version of Expo Go» (Проект несовместим с этой версией Expo Go)

Правильное решение зависит от того, используете ли вы устройство Android, физическое устройство iOS или симулятор. Ознакомьтесь с разделом «Troubleshoot an Expo Go version mismatch» (Устранение проблем с несовместимостью версий Expo Go), чтобы установить совместимую версию или обновить проект.
Как обновить проект с неподдерживаемой версией SDK?

Инструкции по обновлению до конкретной версии SDK можно найти в руководстве «Upgrading Expo SDK».
Каталог React Native

Любая библиотека, совместимая с React Native, будет работать в проекте Expo, если для его создания используется сборка для разработки (development build).

reactnative.directory — это база данных библиотек React Native с функцией поиска. Если нужной вам библиотеки нет в составе Expo SDK, воспользуйтесь этим каталогом, чтобы найти совместимую библиотеку для своего проекта.

## Навигация в приложениях Expo и React Native

Узнайте о рекомендуемом подходе к внедрению навигации в проекты Expo и React Native.

В базовую библиотеку React Native не входит встроенное решение для навигации, поэтому вы можете выбрать ту библиотеку, которая лучше всего отвечает вашим потребностям. Для приложений на Expo и React Native выбор обычно стоит между React Navigation и Expo Router.
Зачем приложениям React Native нужна библиотека навигации

Ядро React Native включает базовые компоненты пользовательского интерфейса, обработку касаний, API устройства и сетевые функции, но не содержит (помимо прочего) средств для работы с хранилищем данных, камерой, картами, большинством датчиков устройства и навигацией! Эти задачи решаются с помощью библиотек, созданных сообществом.
React Navigation

React Navigation — это библиотека навигации на основе компонентов, широко используемая в экосистеме React Native. Она позволяет полностью программно создавать навигаторы различных типов (стековые, вкладочные и выдвижные панели), реализуя сложные сценарии переходов, кастомную анимацию и специфические для приложения паттерны взаимодействия (UX).

Библиотека обеспечивает внешний вид и поведение, характерные для конкретной платформы (с плавной анимацией и поддержкой жестов), предлагает унифицированную маршрутизацию для мобильных устройств и веба, автоматическую поддержку глубоких ссылок (deep links) и типизированные маршруты со статической конфигурацией, а также отличается высокой гибкостью настройки.
React Navigation: Начало работы

Узнайте, как начать работу с React Navigation.
Expo Router (рекомендуется для проектов Expo)

Expo Router — это библиотека маршрутизации на основе файловой структуры для проектов Expo и React Native. Следуя соглашению об организации директории приложения, она преобразует файлы в маршруты и интегрируется с Expo (включая Expo CLI и процесс сборки) без дополнительной настройки. Библиотека также предоставляет такие возможности, как типизированные и динамические маршруты, ленивая сборка (lazy bundling) в режиме разработки, статическая отрисовка для веба и автоматическая поддержка глубоких ссылок.

В новые проекты Expo, созданные с помощью команды `npx create-expo-app@latest`, Expo Router включен по умолчанию.
