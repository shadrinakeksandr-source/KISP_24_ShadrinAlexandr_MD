# Руководство: Работа с React Native и Expo

Введение в руководство по React Native, посвященное созданию универсального приложения для Android, iOS и веба с использованием Expo.

Мы начинаем путь создания универсальных приложений. В этом руководстве мы создадим приложение на базе Expo, которое будет работать на Android, iOS и в вебе — и всё это с использованием единой кодовой базы. Приступим!

    Только начинаете программировать? Вы можете создать это же приложение, используя ИИ-ассистента для написания кода, вместо того чтобы писать его самостоятельно. Ознакомьтесь с руководством «Создание с помощью ИИ» (Build with AI). В нем описывается процесс настройки с нуля.

# О руководстве по React Native и Expo

Цель этого руководства — помочь вам начать работу с Expo и познакомиться с Expo SDK. Мы рассмотрим следующие темы:

    Создание приложения по стандартному шаблону с поддержкой TypeScript
    Реализация макета с двумя экранами и нижней панелью навигации (bottom tabs) с помощью Expo Router
    Разбор структуры макета приложения и его реализация с использованием flexbox
    Использование системных элементов интерфейса каждой платформы для выбора изображения из медиатеки
    Создание модального окна для выбора стикеров с использованием компонентов `<Modal>` и `<FlatList>` из React Native
    Добавление сенсорных жестов для взаимодействия со стикером
    Использование сторонних библиотек для создания снимка экрана и его сохранения на диск
    Учет различий между платформами Android, iOS и веб
    Настройка строки состояния (status bar), заставки (splash screen) и иконки для завершения работы над приложением

Эти темы закладывают основу для понимания принципов создания приложений на Expo. Руководство рассчитано на самостоятельное изучение; его прохождение может занять до двух часов.

Чтобы материал был доступен новичкам, мы разделили руководство на девять глав: вы можете проходить их последовательно или сделать перерыв и вернуться к работе позже. В каждой главе приведены необходимые фрагменты кода, поэтому вы можете либо создавать приложение с нуля, следуя инструкциям, либо копировать и вставлять готовый код.

Прежде чем начать, давайте посмотрим, что именно мы будем создавать. Это приложение под названием StickerSmash, работающее на Android, iOS и в вебе:

    Полный исходный код этого руководства доступен на GitHub.

# Как пользоваться этим руководством

Мы верим в обучение на практике, поэтому в данном руководстве основное внимание уделяется действиям, а не теории. Вы сможете пройти весь путь создания приложения, написав его код с нуля. На протяжении всего руководства важный код или код, изменившийся по сравнению с предыдущими примерами, будет выделен зеленым цветом. Вы можете навести курсор на выделенные фрагменты (на компьютере) или нажать на них (на мобильном устройстве), чтобы узнать подробности об изменениях. Например, код, выделенный в приведенном ниже фрагменте, сопровождается пояснением своего назначения:
import { StyleSheet, Text, View } from 'react-native';

export default function Index() {
  return (
    <View style={styles.container}>
      <Text>Hello world!</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
});

# Создайте свое первое приложение

В этой главе вы узнаете, как создать новый проект Expo.

В этой главе мы научимся создавать новый проект Expo и запускать его.
Смотреть: Создание вашего первого универсального приложения Expo
Смотреть: Создание вашего первого универсального приложения Expo

Создайте новый проект Expo с нуля и запустите его на Android, iOS и в веб-браузере.

# Предварительные требования

4 требования

1.
Expo Go на реальном устройстве

Установите Expo Go на реальное устройство Android или iOS.

2.
Node.js (LTS)

Установите Node.js (версию LTS) на свой компьютер.

3.
Редактор кода

Установите VS Code или любой другой удобный для вас редактор кода или IDE.

4.
Компьютер разработчика с терминалом

macOS, Linux или Windows (с PowerShell и WSL2) и открытым окном терминала.

В этом руководстве предполагается, что вы знакомы с TypeScript и React. Если нет, ознакомьтесь с руководством по TypeScript (TypeScript Handbook) и официальным руководством по React.

# Инициализация нового приложения Expo

Для инициализации нового приложения Expo мы будем использовать `create-expo-app`. Это инструмент командной строки для создания новых проектов React Native. Выполните следующую команду в терминале:
Терминал
npm
yarn
pnpm
bun
npx create-expo-app@latest StickerSmash
Выберите версию Expo SDK > SDK 57
cd StickerSmash

Эта команда создаст новый каталог проекта с именем StickerSmash, используя шаблон по умолчанию. Этот шаблон содержит необходимый базовый код и библиотеки для сборки приложения (включая Expo Router) и позволяет тестировать приложение с помощью Expo Go, установленного на ваших устройствах. По мере необходимости мы будем добавлять дополнительные библиотеки в ходе работы с этим руководством.
Преимущества использования шаблона по умолчанию

    Создает новый проект React Native с установленным пакетом `expo`
    Включает рекомендуемые инструменты, такие как Expo CLI
    Включает навигатор с вкладками (tab navigator) из Expo Router для обеспечения базовой навигации
    Автоматически настроен для запуска проекта на нескольких платформах: Android, iOS и в вебе
    TypeScript настроен по умолчанию

# Загрузка ресурсов
Скачать архив с ресурсами

Мы будем использовать эти ресурсы на протяжении всего руководства. После скачивания архива:

    Распакуйте архив и замените стандартные ресурсы (assets) в директории `your-project-name/assets/images`.
    Откройте директорию проекта в редакторе кода или IDE.

    Запустить сценарий сброса-проекта

В этом уроке мы создадим наше приложение с нуля и поймем основы добавления файловой навигации. Давайте запустим reset-project скрипт для удаления кода шаблона:
Терминал
npm
пряжа
pnpm
булочка

npm run reset-project

После выполнения вышеуказанной команды в каталоге src/app осталось два файла (index.tsx и _layout.tsx). Предыдущие файлы из каталога src (включая componentsкомпоненты, константы и зацепки) перемещаются в диаграмму по сценарию. Мы создадим наши собственные каталоги и составные файлы по мере продвижения.
Что делает reset-projectСценарий делает?

reset-project script resets the src/app directory structure in a project and moves the previous boilerplate files from the project's src directory to another sub-directory called example. We can delete it since it is not part of our main app's structure.

# Запуск приложения на мобильном и веб-сайте

В каталоге проекта запустите следующую команду для запуска сервера разработки с терминала:
Терминал
npm
пряжа
pnpm
булочка

npx expo start

После выполнения вышеуказанной команды:

    Завершится сервер разработки, и вы увидите QR-код внутри окна терминала.
    Сканируйте этот QR-код, чтобы открыть приложение на устройстве. На Android используйте опцию Expo Go > Scan QR-кода. На iOS используйте приложение камеры по умолчанию.
    Чтобы запустить веб-приложение, нажмите W В терминале. Он откроет веб-приложение в веб-браузере по умолчанию.

# Редактировать индексный экран

The src/app/index.tsx файл определяет текст, отображаемый на экране приложения. Это точка входа нашего приложения и выполняет, когда сервер разработки начинается. Он использует основные компоненты React Native, такие как <View> и <Text> для отображения фона и текста.

Стили, применяемые к этим компонентам, используют объекты JavaScript, а не CSS, который используется в Интернете. Тем не менее, многие свойства будут выглядеть знакомыми, если вы ранее использовали CSS в Интернете. Большинство реагированных народные компоненты принимают style реквизит, который принимает объект JavaScript в качестве его значения. Для получения более подробной информации см. Стиль в React Native.

Давайте изменим экран src/app/index.tsx:

    Импорт StyleSheet от react-native и создать a styles Возражает, чтобы определить наши пользовательские стили.
    Add a styles.container.backgroundColor собственность для <View> с ценностью #25292e. Это меняет цвет фона.
    Replace the default value of <Text> with "Home screen".
    Add a styles.text.color собственность для <Text> с ценностью #fff (white) to change the text color.

import { Text, View,  StyleSheet } from 'react-native';

export default function Index() {
  return (
    <View style={styles.container}>
      <Text style={styles.text}>Home screen</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#25292e',
    alignItems: 'center',
    justifyContent: 'center',
  },
  text: {
    color: '#fff',
  },
});

Как только вы сохраняете свои изменения, они отправляются и применяются к запущенным приложениям, подключенным к серверу разработки: